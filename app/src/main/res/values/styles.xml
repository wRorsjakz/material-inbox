<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:statusBarColor">?android:attr/windowBackground</item>
        <item name="android:windowLightStatusBar">true</item>

        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>
        
    </style>

    <!-- Bottom sheet Dialog theme -->

    <style name="AppBottomSheetDialogTheme"
        parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle"
        parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/rounded_bottomsheet_background</item>
    </style>

    <!-- CollapsingToolbar title style -->

    <style name="ExpandedCollapsingToolbar" parent="@android:style/TextAppearance.Medium">
        <item name="android:textSize">26sp</item>
        <item name="android:textColor">#0e0e0e</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="CollapsedToolbar" parent="@android:style/TextAppearance.Medium">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">#0e0e0e</item>
        <item name="android:textStyle">bold</item>
    </style>


    <!--BottomSheetFragment Sliding Animation -->

    <style name="DialogAnimation">
        <item name="android:windowExitAnimation">@anim/bottom_dialog_slide_down</item>
        <item name="android:windowEnterAnimation">@anim/bottom_dialog_slide_up</item>
    </style>

</resources>
